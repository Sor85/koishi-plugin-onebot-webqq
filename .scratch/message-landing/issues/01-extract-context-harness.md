# 01 — Koishi 上下文替身提取到共享测试脚手架

**What to build:** 让 Koishi 上下文替身可以被多个测试文件共用。它现在住在仓库里最大的那个测试文件内部（两千多行、顶层十几个读文件操作），因此后续任何需要它的测试只有三条路：再抄一份、从那个测试文件 import、或者放弃在那一层测。本 ticket 把它提到共享测试脚手架目录，原文件改成 import。

这是 02 的前置。纯移动，替身的能力一项不加一项不减。

**Blocked by:** 无 — 可以立即开工。

**Status:** resolved

**参考:** `.scratch/message-landing/spec.md` 的「Koishi 上下文替身提取到共享脚手架」一节。这与候选 ⑨（内存 Koishi 控制台脚手架被复制三份）**有意重叠**：提取本身就是那个候选的一部分，本轮先把被两张票需要的这一份做掉。

- [x] 上下文替身住在共享测试脚手架目录，可被任意测试文件 import
- [x] 替身的能力一项不变：事件注册、可选控制台（含监听器与入口收集器）、可选数据库与模型扩展、bot 列表、日志器
- [x] 原测试文件改成 import 而不是保留一份副本；它的全部断言仍然通过
- [x] 生产代码零改动
- [x] 候选 ⑨ 提到的另外两处重复本轮不碰
- [x] 全量测试通过
