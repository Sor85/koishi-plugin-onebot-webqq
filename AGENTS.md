# Agent 工作规则

## Agent skills

### Issue tracker

Issues 和 PRD 发布到 GitHub Issues，并通过 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库使用 single-context 布局：根目录 `CONTEXT.md` 和 `docs/adr/`。见 `docs/agents/domain.md`。
