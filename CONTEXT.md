# OneBot WebQQ UI

This context defines the project-specific language for the floating WebQQ capsule in the Koishi console.

## Language

### 界面

**主胶囊**:
Koishi 控制台右下角的浮动 WebQQ 入口外壳，承载当前机器人头像和摘要信息。
_Avoid_: 大胶囊, 外层按钮

**主胶囊表面**:
主胶囊的统一视觉底层，负责背景、边框和阴影，头像小胶囊与胶囊摘要文字不应各自绘制独立表面。
_Avoid_: 文字阴影, 头像阴影

**紧凑胶囊阴影**:
主胶囊表面的较窄阴影模式，用于降低胶囊外轮廓的扩张感。
_Avoid_: 当前样式, 新样式

**头像小胶囊**:
主胶囊中包住机器人头像或头像栈的左侧区域，是头像与主胶囊圆角关系的视觉参照。
_Avoid_: 左侧容器, 头像区域

**头像引导光圈**:
头像外缘上的图形引导提示，用来提示用户可通过头像打开 WebQQ 观察窗。
_Avoid_: 引导圈, 头像提示

**头像余量徽标**:
头像栈折叠态中表示未直接露出的机器人头像数量的圆形徽标；展开和折叠时应由对应头像槽位承接，作为头像栈运动的一部分，而不是静态背景板。
_Avoid_: 余量背景, 背景板

**胶囊摘要文字**:
主胶囊右侧显示机器人昵称和当前状态的两行文字，整体应与头像小胶囊在垂直方向保持等距对齐。
_Avoid_: 右侧文字, 正文区域

**胶囊对话状态**:
胶囊摘要文字中表示当前对话目标的状态，固定语义前后缀应始终可见，目标名称可以被省略。
_Avoid_: 活动文本, 对话整句

**等距内缩**:
头像小胶囊中的头像对齐规则，头像左侧留白应与上下留白一致。
_Avoid_: 视觉居中, 只调高度

**一级工作区**:
WebQQ 观察窗内持续存在的会话列表、聊天区、消息列表、群资料栏和输入区。
_Avoid_: 主面板, 主窗口区域

**实体二级面**:
关闭毛玻璃外观时的不透明浮层，不通过 `backdrop-filter` 模糊后方内容。
_Avoid_: 实心弹窗, 不透明卡片

**雾化二级面**:
启用毛玻璃外观时的半透明浮层，通过 `backdrop-filter` 模糊后方内容。
_Avoid_: 毛玻璃卡片, 玻璃弹窗

**Backdrop Root 边界**:
带 `backdrop-filter` 的元素形成的采样边界，其内部后代和覆盖其上的浮层都采不到边界外的内容。
_Avoid_: 模糊祖先, 滤镜根

**排版基线**:
插件在自己的根与每个 body 级浮层根上显式声明的基准字号与行高，用来阻断宿主页面与浏览器默认值的继承。
_Avoid_: 全局字号, reset, 默认字号

**字号标度**:
集中定义的有限档位字号令牌，界面所有文字从其中取值；跟随容器尺寸的字号与图标字形大小都不属于标度。
_Avoid_: 字号变量, 字体大小令牌, font scale

### 输入区

**输入区**:
一级工作区底部用于撰写并附加待发内容的区域，持有草稿、提及菜单与待发附件。
_Avoid_: 发送栏, 编辑器, composer

**草稿**:
输入区中尚未发送的内容，由文字与原子提及两种 token 顺序组成。
_Avoid_: 输入内容, 草稿文本

**待发附件**:
已选入输入区但尚未随消息发出的图片、视频或文件。
_Avoid_: 上传列表, 待上传文件

**发送区占位**:
消息列表为输入区在底部预留的高度，随回复条与待发附件的出现而变化。
_Avoid_: 底部留白, 发送栏高度

### 查找与筛选

**会话内查找**:
在当前会话的历史消息里按查找条件定位消息，包括尚未取回到界面上的更早消息。
_Avoid_: 搜索, 消息搜索, 全局搜索

**查找条件**:
一次会话内查找的关键字与日期范围组合；两者都为空时不构成一次查找。
_Avoid_: 搜索词, 过滤器, query

**命中**:
满足查找条件的消息。
_Avoid_: 搜索结果, 匹配项

**续查游标**:
会话内查找继续向更早历史推进的位置。
_Avoid_: 分页参数, offset, beforeSequence

**联系人筛选**:
在侧边栏已列出的会话与联系人里就地过滤，不涉及尚未取回的内容。
_Avoid_: 搜索, 联系人搜索

**成员筛选**:
在群资料栏已列出的群成员里就地过滤。
_Avoid_: 搜索, 成员搜索

### 对话框

**输入对话框**:
征询一个值的二级面；确认后立即关闭，不等动作完成。
_Avoid_: 弹窗, 表单框, prompt

**确认对话框**:
征询一次确认的二级面；处理期间保持打开，动作失败时留在原地供重试。
_Avoid_: 弹窗, 警告框, confirm

### 配置

**配置规格**:
插件全部配置项的唯一权威声明，逐项记录键名、默认值和是否镜像到前端。
_Avoid_: 配置表, 配置清单, schema 定义

**镜像配置项**:
配置规格中标注为需要送到前端的配置项；未标注的只在服务端消费。
_Avoid_: 下发字段, 客户端配置

**配置镜像**:
前端持有的一份镜像配置项副本，由服务端下发写入并供界面读取，不参与配置的权威判定。
_Avoid_: 前端配置, settings

### 运行环境

**虚拟 OneBot 机器人**:
由其他插件注册进 Koishi、platform 为 `onebot`、对 UI 隐藏、action 通道由该插件在自有场景中实现的机器人。
_Avoid_: 假机器人, 沙盒机器人, mock bot

**开发者模拟环境**:
把虚拟 OneBot 机器人纳入 WebQQ 的运行模式，场景由提供方插件持有；读写走真实 OneBot action，实时消息走真实 Koishi 事件。开启期间真实机器人不出现在 WebQQ 里。
_Avoid_: 内存模拟, 预设场景, 测试模式
